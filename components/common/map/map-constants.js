export const MAP_DEFAULT_OPTIONS = {
  minZoom: 1,
  maxZoom: 15,
  zoom: 1,
  center: [40, 0]
};

export const MARKER_STYLES = {
  default: { outline: 'none' },
  hover: { cursor: 'pointer' }
};

export default {
  MARKER_STYLES,
  MAP_DEFAULT_OPTIONS
};
